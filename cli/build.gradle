dependencies {
    compile project(':journey-processor')
}
jar {
    manifest {
        attributes 'Main-Class': 'com.atmaram.jp.cli.Main'
    }
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
